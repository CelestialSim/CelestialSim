# Documentation

To build the docs locally:
1. Run `cargo doc -p celestialsim --no-deps`;
2. Open `target/doc/celestialsim/index.html`.

For a single command that also opens the browser, run `cargo doc -p celestialsim --no-deps --open`.
